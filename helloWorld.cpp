// if a line starts with #, it means it is a preprocessor directive
// preprocessor reads your program before it is compiled and only executes those lines with #
#include <iostream>  //iostream contains code that allows a C++ program to display output and read input from keyboard
using namespace std; // this tells us that we will be using entities from namespace called std.

int main()
{
  cout << "Hello World";
  return 0;
}